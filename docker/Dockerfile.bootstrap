# syntax=docker/dockerfile:1.7

FROM node:26.8-bookworm-slim@sha256:cd9f682fa2885cd1056e830424764158570061c59736a1da836bc3d73df095ae

RUN apt-get update \
  && apt-get install --yes --no-install-recommends ca-certificates openssl \
  && rm -rf /var/lib/apt/lists/*
COPY runtime-bootstrap.sh /usr/local/bin/runtime-bootstrap
RUN chmod 0555 /usr/local/bin/runtime-bootstrap
ENTRYPOINT ["/usr/local/bin/runtime-bootstrap"]
