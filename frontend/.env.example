# Browser calls use same-origin /auth/* plus exact JWT inquiry/management paths
# through http://localhost:3000. Native Next.js proxies only those paths;
# API-key orchestration remains internal.
# Update requests use the fixed public https://api.getdefy.co/contact/create endpoint
# through an isolated client without credentials; no environment override is needed.
BASE_URL=http://localhost:3002
